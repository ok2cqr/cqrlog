name: Makefile CI

on:
  push:
    branches: [ "*" ]

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
    - uses: actions/checkout@v3

    - name: Install Dependencies
      run: make dependencies

    - name: Run Build
      run: make cqrlog
